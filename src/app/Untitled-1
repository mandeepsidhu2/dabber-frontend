<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand">Tabber</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
  
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item active">
          <a class="nav-link">Home <span class="sr-only">(current)</span></a>
        </li>
      </ul>

      <div *ngIf=!loggedIn class="form-inline my-2 my-lg-0">
          <ul  class=" navbar-nav mr-auto">
              <li  class="nav-item">
                  <fa-icon class="fa-2x" (click)="signInWithGoogle()" [icon]="faGoogle"></fa-icon>
              </li> 
          </ul>
      </div>

      <div *ngIf=loggedIn class="form-inline my-2 my-lg-0">
          <ul  class="nav navbar-nav mr-auto">
              <li class="nav-item" >
                <h3>{{user.name}}</h3>
              </li>
              <li  class="nav-item" >
                      <button type="button" (click)="signOut()" class="btn btn-outline-danger">Logout</button>                
              </li> 
        </ul>
      </div>
    </div>
  </nav>

<!-- <nav class="navbar navbar navbar-default navbar-expand-md navbar-default shadow-sm p-3 mb-5 bg-white rounded ">
  
    <div class="navbar-header">
        <h1 class="navbar-brand">Tabber</h1>
      </div>

    <div *ngIf=!loggedIn class="navbar-collapse collapse w-100 order-3 dual-collapse2">
        
        <ul  class="nav navbar-nav ml-auto">
            <li  class="nav-item">
                <fa-icon class="fa-2x" (click)="signInWithGoogle()" [icon]="faGoogle"></fa-icon>
            </li> 
        </ul>
    </div>
    <div  *ngIf=loggedIn class="navbar-collapse collapse w-100 order-3 dual-collapse2">
            <ul  class="nav navbar-nav ml-auto">
                    <li class="nav-item" style="margin-right: 2em;">
                      <h3>{{user.name}}</h3>
                    </li>
                    <li  class="nav-item">
                            <button type="button" (click)="signOut()" class="btn btn-outline-danger">Logout</button>                
                    </li> 
            </ul>
    </div>
  
</nav>


<app-homepage></app-homepage> -->